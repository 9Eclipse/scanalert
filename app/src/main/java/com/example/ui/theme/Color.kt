package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Perfume / ScentAlert Luxury Color Palette
val MatteCharcoal = Color(0xFF0F0F11)
val DarkVelvet = Color(0xFF18181C)
val LightVelvet = Color(0xFF232329)

val RoseGold = Color(0xFFD4AF37) // Golden tone for luxury branding
val RoseGoldLight = Color(0xFFF3E5AB) // Muted high-contrast gold glow
val GoldAccent = Color(0xFFC5A059) // Brushed gold for borders/dividers

val NeutralCream = Color(0xFFF9F6F0) // Warm linen/cream for readable light theme text
val NeutralMuted = Color(0xFFA0A0AB) // Muted grey for subtexts

// Deal Score Indicator Colors (Section 13)
val ScentExceptional = Color(0xFFD4AF37) // 90-100 Deal Score (Gold)
val ScentVeryGood = Color(0xFF10B981) // 75-89 Deal Score (Emerald Green)
val ScentGood = Color(0xFFF59E0B) // 60-74 Deal Score (Amber/Orange)
val ScentRegular = Color(0xFF6B7280) // < 60 Deal Score (Slate Muted Gray)
